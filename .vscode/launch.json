{
  "configurations": [
    {
      "name": "Attach to Chrome",
      "port": 5501,
      "request": "launch",
      "url": "localhost:5501/${relativeFile}",
      "type": "chrome",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
